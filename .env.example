# Ambil dari Supabase Dashboard -> Project Settings -> API
SUPABASE_URL=https://qojdhztqqzolbpqrtixp.supabase.co
SUPABASE_SERVICE_ROLE_KEY=isi-dengan-service-role-key-anda

# PENTING: SUPABASE_SERVICE_ROLE_KEY punya akses penuh ke database (melewati RLS).
# JANGAN pernah taruh di kode frontend / commit ke Git.
# Hanya diset sebagai Environment Variable di Vercel (server-side) dan di .env.local lokal (yang sudah di-.gitignore).
